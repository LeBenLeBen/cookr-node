# URLs (not necessary in development)
API_ORIGIN=https://api.cookr.ch
APP_ORIGIN=https://www.cookr.ch
COOKIES_DOMAIN=cookr.ch

# Secrets can be generated with `openssl rand 64 | base64`
ADMIN_JWT_SECRET=
JWT_SECRET=

# AWS credentials to store images in S3
AWS_ACCESS_KEY_ID=
AWS_ACCESS_SECRET=
AWS_REGION=
AWS_BUCKET=

# Algolia config
ALGOLIA_APPLICATION_ID=
ALGOLIA_ADMIN_API_KEY=
